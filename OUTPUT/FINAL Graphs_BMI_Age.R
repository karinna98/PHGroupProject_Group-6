# Question 4.II

hist(hints.data$Age, main = "histogram of age") # create histogram of 'Age' distribution

boxplot (hints.data$BMI, main = "BMI Boxplot") # create boxplot of 'BMI'

summary(hints.data$BMI) # provide statistical summary of 'BMI' 
